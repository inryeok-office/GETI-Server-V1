package team.inreok.getiserver.domain.program.entity.type

// Program 신청·취소 Action API(POST .../application-actions) 요청의 action 값이다(요구사항 4절/11절).
enum class ProgramApplicationAction {
    APPLY,
    CANCEL,
}
